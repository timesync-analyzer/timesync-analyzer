node_timeout: 30s
slider:
  calculate_interval: 10s
  observation_interval: 1h
zmq:
  timeout: 490ms
storage:
  batch:
    max_size: 500
    flush_interval: 1s
worker:
  num_workers: 4
  queue_size: 1000
