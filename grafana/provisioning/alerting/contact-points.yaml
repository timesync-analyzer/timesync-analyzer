apiVersion: 1

contactPoints:
  - orgId: 1
    name: reportd
    receivers:
      - uid: reportd-webhook
        type: webhook
        disableResolveMessage: true
        settings:
          url: http://reportd:8080/api/grafana/alerts
          httpMethod: POST
          authorization_scheme: Bearer
          authorization_credentials: $REPORT_TOKEN
